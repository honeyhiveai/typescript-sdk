/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metricsUpdateMetric } from "../../funcs/metricsUpdateMetric.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.MetricEdit$inboundSchema,
};

export const tool$metricsUpdateMetric: ToolDefinition<typeof args> = {
  name: "metrics-update-metric",
  description: `Update an existing metric

Edit a metric`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await metricsUpdateMetric(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
