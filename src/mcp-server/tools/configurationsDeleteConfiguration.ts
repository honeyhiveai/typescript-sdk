/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { configurationsDeleteConfiguration } from "../../funcs/configurationsDeleteConfiguration.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  id: z.string(),
};

export const tool$configurationsDeleteConfiguration: ToolDefinition<
  typeof args
> = {
  name: "configurations-delete-configuration",
  description: `Delete a configuration`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await configurationsDeleteConfiguration(
      client,
      args.id,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
