/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sessionStartSession } from "../../funcs/sessionStartSession.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.StartSessionRequestBody$inboundSchema,
};

export const tool$sessionStartSession: ToolDefinition<typeof args> = {
  name: "session-start-session",
  description: `Start a new session`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sessionStartSession(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
