/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { eventsGetEvents } from "../../funcs/eventsGetEvents.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetEventsRequestBody$inboundSchema,
};

export const tool$eventsGetEvents: ToolDefinition<typeof args> = {
  name: "events-get-events",
  description: `Retrieve events based on filters`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await eventsGetEvents(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
