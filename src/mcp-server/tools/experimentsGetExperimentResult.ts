/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { experimentsGetExperimentResult } from "../../funcs/experimentsGetExperimentResult.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  runId: z.string(),
  projectId: z.string(),
  aggregateFunction: operations.AggregateFunction$inboundSchema.optional(),
};

export const tool$experimentsGetExperimentResult: ToolDefinition<typeof args> =
  {
    name: "experiments-get-experiment-result",
    description: `Retrieve experiment result`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await experimentsGetExperimentResult(
        client,
        args.runId,
        args.projectId,
        args.aggregateFunction,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
