/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { datapointsCreateDatapoint } from "../../funcs/datapointsCreateDatapoint.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CreateDatapointRequest$inboundSchema,
};

export const tool$datapointsCreateDatapoint: ToolDefinition<typeof args> = {
  name: "datapoints-create-datapoint",
  description: `Create a new datapoint`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await datapointsCreateDatapoint(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
