/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { configurationsUpdateConfiguration } from "../../funcs/configurationsUpdateConfiguration.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  putConfigurationRequest: components.PutConfigurationRequest$inboundSchema,
  id: z.string(),
};

export const tool$configurationsUpdateConfiguration: ToolDefinition<
  typeof args
> = {
  name: "configurations-update-configuration",
  description: `Update an existing configuration`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await configurationsUpdateConfiguration(
      client,
      args.putConfigurationRequest,
      args.id,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
