/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { eventsCreateModelEvent } from "../../funcs/eventsCreateModelEvent.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateModelEventRequestBody$inboundSchema,
};

export const tool$eventsCreateModelEvent: ToolDefinition<typeof args> = {
  name: "events-create-model-event",
  description: `Create a new model event

Please refer to our instrumentation guide for detailed information`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await eventsCreateModelEvent(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
