/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { datasetsDeleteDataset } from "../../funcs/datasetsDeleteDataset.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  datasetId: z.string(),
};

export const tool$datasetsDeleteDataset: ToolDefinition<typeof args> = {
  name: "datasets-delete-dataset",
  description: `Delete a dataset`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await datasetsDeleteDataset(
      client,
      args.datasetId,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
