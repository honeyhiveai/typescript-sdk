{
  "dependencies": {
    "@opentelemetry/api": "^1.8.0",
    "@traceloop/node-server-sdk": "^0.8.4",
    "@types/uuid": "^9.0.8",
    "axios": "^1.6.7",
    "honeyhive": "file:..",
    "jest": "^29.7.0",
    "openai": "^4.28.4",
    "ts-jest": "^29.1.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3",
    "uuid": "^9.0.1"
  },
  "scripts": {
    "test": "cd .. && rm -rf node_modules && npm run build && cd tests && rm -rf node_modules && npm run build && npx ts-node $1",
    "build": "npm install && tsc",
    "ts-node": "ts-node"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/mocha": "^10.0.6",
    "@types/node": "^20.11.27"
  }
}
